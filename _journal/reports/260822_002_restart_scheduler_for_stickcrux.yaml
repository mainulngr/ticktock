intent: Restart the scheduler so it picks up the newly added stickcrux channel.
actions_taken:
  - Stopped the running ticktock watch process and restarted it with `just restart` (PID 1898046).
  - Seeded stickcrux state by listing 50 videos and attempting one download to populate the pending count.
  - Removed the 50-video list cache for stickcrux so the next scheduler cycle will do a full channel listing.
  - Confirmed stickcrux now has 49 pending videos in state.db and the scheduler is alive.
files_modified:
  - data/scheduler.pid (auto-generated by justfile)
  - data/list_cache/stickcrux.json (removed to force full re-list)
  - _journal/reports/260822_002_restart_scheduler_for_stickcrux.yaml
notes: "The scheduler did pick up stickcrux on the next cycle and re-listed it. Multiple download attempts then failed with: [TikTok] Unable to extract universal data for rehydration / Unexpected response from webpage request. This appears to be a yt-dlp/TikTok extractor issue for this channel, not a scheduler or cookie issue. The scheduler will mark these as failed and move on; if it persists across channels or versions, consider updating yt-dlp."

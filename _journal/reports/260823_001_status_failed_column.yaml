intent: "Fix misleading `just status` output where channels with failed downloads looked done."
actions_taken:
  - Queried data/state.db and confirmed gaps (listed - done) are `failed=1` rows, not missing pending.
  - Updated `ticktock/status.py` to count and display `failed` as a separate column.
  - Changed status labels and percent so `failed` but zero-pending channels show `blocked` with real `done/listed` percent.
  - Updated `.windsurf/rules/workspace.md` to reflect the new status columns.
  - Committed the change.
files_modified:
  - ticktock/status.py
  - .windsurf/rules/workspace.md
notes: "1954 total failed videos across 13 channels. These are mostly `yt-dlp download failed (code 1)` errors from 21-22 Aug. They are not being retried because failed=1 removes them from pending. Decide whether to implement cooldown-based retry or a manual `just retry-failed` command."
